export * from '../models/RobloxApiAvatarModelsAssetIdListModel';
export * from '../models/RobloxApiAvatarModelsAssetMetaModelV1';
export * from '../models/RobloxApiAvatarModelsAssetModelV2';
export * from '../models/RobloxApiAvatarModelsAssetTypeModel';
export * from '../models/RobloxApiAvatarModelsAssetTypeRulesModel';
export * from '../models/RobloxApiAvatarModelsAvatarApiSuccessResponse';
export * from '../models/RobloxApiAvatarModelsAvatarFilteredPageResponseRobloxApiAvatarModelsOutfitModel';
export * from '../models/RobloxApiAvatarModelsAvatarMetadataModel';
export * from '../models/RobloxApiAvatarModelsAvatarModelV2';
export * from '../models/RobloxApiAvatarModelsAvatarRulesModel';
export * from '../models/RobloxApiAvatarModelsAvatarRulesModelAccessoryRefinementLowerBounds';
export * from '../models/RobloxApiAvatarModelsBodyColorModel';
export * from '../models/RobloxApiAvatarModelsBodyColorsModel';
export * from '../models/RobloxApiAvatarModelsDefaultClothingAssets';
export * from '../models/RobloxApiAvatarModelsEmoteResponseModel';
export * from '../models/RobloxApiAvatarModelsGameStartInfoResponse';
export * from '../models/RobloxApiAvatarModelsOutfitDetailsModel';
export * from '../models/RobloxApiAvatarModelsOutfitModel';
export * from '../models/RobloxApiAvatarModelsOutfitUpdateModelV1';
export * from '../models/RobloxApiAvatarModelsPlayerAvatarTypeModel';
export * from '../models/RobloxApiAvatarModelsRecentItemModel';
export * from '../models/RobloxApiAvatarModelsScaleRulesModel';
export * from '../models/RobloxApiAvatarModelsUniverseAvatarAssetOverrideResponseModel';
export * from '../models/RobloxApiAvatarModelsWearResponseModel';
export * from '../models/RobloxAvatarcoreCommonModelsAccessoryPositionModel';
export * from '../models/RobloxAvatarcoreCommonModelsAccessoryRefinementModel';
export * from '../models/RobloxAvatarcoreCommonModelsAccessoryRotationModel';
export * from '../models/RobloxAvatarcoreCommonModelsAccessoryScaleModel';
export * from '../models/RobloxAvatarcoreSharedV3Beta1AssetPosition';
export * from '../models/RobloxAvatarcoreSharedV3Beta1AssetRotation';
export * from '../models/RobloxAvatarcoreSharedV3Beta1AssetScale';
export * from '../models/RobloxWebResponsesAvatarScaleModel';
export * from '../models/RobloxWebWebAPIModelsApiLegacyPageResponseRobloxApiAvatarModelsRecentItemModel';

import { RobloxApiAvatarModelsAssetIdListModel } from '../models/RobloxApiAvatarModelsAssetIdListModel';
import { RobloxApiAvatarModelsAssetMetaModelV1 } from '../models/RobloxApiAvatarModelsAssetMetaModelV1';
import { RobloxApiAvatarModelsAssetModelV2 } from '../models/RobloxApiAvatarModelsAssetModelV2';
import { RobloxApiAvatarModelsAssetTypeModel } from '../models/RobloxApiAvatarModelsAssetTypeModel';
import { RobloxApiAvatarModelsAssetTypeRulesModel } from '../models/RobloxApiAvatarModelsAssetTypeRulesModel';
import { RobloxApiAvatarModelsAvatarApiSuccessResponse } from '../models/RobloxApiAvatarModelsAvatarApiSuccessResponse';
import { RobloxApiAvatarModelsAvatarFilteredPageResponseRobloxApiAvatarModelsOutfitModel } from '../models/RobloxApiAvatarModelsAvatarFilteredPageResponseRobloxApiAvatarModelsOutfitModel';
import { RobloxApiAvatarModelsAvatarMetadataModel } from '../models/RobloxApiAvatarModelsAvatarMetadataModel';
import { RobloxApiAvatarModelsAvatarModelV2 , RobloxApiAvatarModelsAvatarModelV2PlayerAvatarTypeEnum        } from '../models/RobloxApiAvatarModelsAvatarModelV2';
import { RobloxApiAvatarModelsAvatarRulesModel, RobloxApiAvatarModelsAvatarRulesModelPlayerAvatarTypesEnum    , RobloxApiAvatarModelsAvatarRulesModelAccessoryRefinementTypesEnum            } from '../models/RobloxApiAvatarModelsAvatarRulesModel';
import { RobloxApiAvatarModelsAvatarRulesModelAccessoryRefinementLowerBounds } from '../models/RobloxApiAvatarModelsAvatarRulesModelAccessoryRefinementLowerBounds';
import { RobloxApiAvatarModelsBodyColorModel } from '../models/RobloxApiAvatarModelsBodyColorModel';
import { RobloxApiAvatarModelsBodyColorsModel } from '../models/RobloxApiAvatarModelsBodyColorsModel';
import { RobloxApiAvatarModelsDefaultClothingAssets } from '../models/RobloxApiAvatarModelsDefaultClothingAssets';
import { RobloxApiAvatarModelsEmoteResponseModel } from '../models/RobloxApiAvatarModelsEmoteResponseModel';
import { RobloxApiAvatarModelsGameStartInfoResponse } from '../models/RobloxApiAvatarModelsGameStartInfoResponse';
import { RobloxApiAvatarModelsOutfitDetailsModel } from '../models/RobloxApiAvatarModelsOutfitDetailsModel';
import { RobloxApiAvatarModelsOutfitModel } from '../models/RobloxApiAvatarModelsOutfitModel';
import { RobloxApiAvatarModelsOutfitUpdateModelV1     , RobloxApiAvatarModelsOutfitUpdateModelV1OutfitTypeEnum   } from '../models/RobloxApiAvatarModelsOutfitUpdateModelV1';
import { RobloxApiAvatarModelsPlayerAvatarTypeModel, RobloxApiAvatarModelsPlayerAvatarTypeModelPlayerAvatarTypeEnum   } from '../models/RobloxApiAvatarModelsPlayerAvatarTypeModel';
import { RobloxApiAvatarModelsRecentItemModel  , RobloxApiAvatarModelsRecentItemModelTypeEnum     } from '../models/RobloxApiAvatarModelsRecentItemModel';
import { RobloxApiAvatarModelsScaleRulesModel } from '../models/RobloxApiAvatarModelsScaleRulesModel';
import { RobloxApiAvatarModelsUniverseAvatarAssetOverrideResponseModel } from '../models/RobloxApiAvatarModelsUniverseAvatarAssetOverrideResponseModel';
import { RobloxApiAvatarModelsWearResponseModel } from '../models/RobloxApiAvatarModelsWearResponseModel';
import { RobloxAvatarcoreCommonModelsAccessoryPositionModel } from '../models/RobloxAvatarcoreCommonModelsAccessoryPositionModel';
import { RobloxAvatarcoreCommonModelsAccessoryRefinementModel } from '../models/RobloxAvatarcoreCommonModelsAccessoryRefinementModel';
import { RobloxAvatarcoreCommonModelsAccessoryRotationModel } from '../models/RobloxAvatarcoreCommonModelsAccessoryRotationModel';
import { RobloxAvatarcoreCommonModelsAccessoryScaleModel } from '../models/RobloxAvatarcoreCommonModelsAccessoryScaleModel';
import { RobloxAvatarcoreSharedV3Beta1AssetPosition } from '../models/RobloxAvatarcoreSharedV3Beta1AssetPosition';
import { RobloxAvatarcoreSharedV3Beta1AssetRotation } from '../models/RobloxAvatarcoreSharedV3Beta1AssetRotation';
import { RobloxAvatarcoreSharedV3Beta1AssetScale } from '../models/RobloxAvatarcoreSharedV3Beta1AssetScale';
import { RobloxWebResponsesAvatarScaleModel } from '../models/RobloxWebResponsesAvatarScaleModel';
import { RobloxWebWebAPIModelsApiLegacyPageResponseRobloxApiAvatarModelsRecentItemModel } from '../models/RobloxWebWebAPIModelsApiLegacyPageResponseRobloxApiAvatarModelsRecentItemModel';

/* tslint:disable:no-unused-variable */
let primitives = [
                    "string",
                    "boolean",
                    "double",
                    "integer",
                    "long",
                    "float",
                    "number",
                    "any"
                 ];

let enumsMap: Set<string> = new Set<string>([
    "RobloxApiAvatarModelsAvatarModelV2PlayerAvatarTypeEnum",
    "RobloxApiAvatarModelsAvatarRulesModelPlayerAvatarTypesEnum",
    "RobloxApiAvatarModelsAvatarRulesModelAccessoryRefinementTypesEnum",
    "RobloxApiAvatarModelsOutfitUpdateModelV1OutfitTypeEnum",
    "RobloxApiAvatarModelsPlayerAvatarTypeModelPlayerAvatarTypeEnum",
    "RobloxApiAvatarModelsRecentItemModelTypeEnum",
]);

let typeMap: {[index: string]: any} = {
    "RobloxApiAvatarModelsAssetIdListModel": RobloxApiAvatarModelsAssetIdListModel,
    "RobloxApiAvatarModelsAssetMetaModelV1": RobloxApiAvatarModelsAssetMetaModelV1,
    "RobloxApiAvatarModelsAssetModelV2": RobloxApiAvatarModelsAssetModelV2,
    "RobloxApiAvatarModelsAssetTypeModel": RobloxApiAvatarModelsAssetTypeModel,
    "RobloxApiAvatarModelsAssetTypeRulesModel": RobloxApiAvatarModelsAssetTypeRulesModel,
    "RobloxApiAvatarModelsAvatarApiSuccessResponse": RobloxApiAvatarModelsAvatarApiSuccessResponse,
    "RobloxApiAvatarModelsAvatarFilteredPageResponseRobloxApiAvatarModelsOutfitModel": RobloxApiAvatarModelsAvatarFilteredPageResponseRobloxApiAvatarModelsOutfitModel,
    "RobloxApiAvatarModelsAvatarMetadataModel": RobloxApiAvatarModelsAvatarMetadataModel,
    "RobloxApiAvatarModelsAvatarModelV2": RobloxApiAvatarModelsAvatarModelV2,
    "RobloxApiAvatarModelsAvatarRulesModel": RobloxApiAvatarModelsAvatarRulesModel,
    "RobloxApiAvatarModelsAvatarRulesModelAccessoryRefinementLowerBounds": RobloxApiAvatarModelsAvatarRulesModelAccessoryRefinementLowerBounds,
    "RobloxApiAvatarModelsBodyColorModel": RobloxApiAvatarModelsBodyColorModel,
    "RobloxApiAvatarModelsBodyColorsModel": RobloxApiAvatarModelsBodyColorsModel,
    "RobloxApiAvatarModelsDefaultClothingAssets": RobloxApiAvatarModelsDefaultClothingAssets,
    "RobloxApiAvatarModelsEmoteResponseModel": RobloxApiAvatarModelsEmoteResponseModel,
    "RobloxApiAvatarModelsGameStartInfoResponse": RobloxApiAvatarModelsGameStartInfoResponse,
    "RobloxApiAvatarModelsOutfitDetailsModel": RobloxApiAvatarModelsOutfitDetailsModel,
    "RobloxApiAvatarModelsOutfitModel": RobloxApiAvatarModelsOutfitModel,
    "RobloxApiAvatarModelsOutfitUpdateModelV1": RobloxApiAvatarModelsOutfitUpdateModelV1,
    "RobloxApiAvatarModelsPlayerAvatarTypeModel": RobloxApiAvatarModelsPlayerAvatarTypeModel,
    "RobloxApiAvatarModelsRecentItemModel": RobloxApiAvatarModelsRecentItemModel,
    "RobloxApiAvatarModelsScaleRulesModel": RobloxApiAvatarModelsScaleRulesModel,
    "RobloxApiAvatarModelsUniverseAvatarAssetOverrideResponseModel": RobloxApiAvatarModelsUniverseAvatarAssetOverrideResponseModel,
    "RobloxApiAvatarModelsWearResponseModel": RobloxApiAvatarModelsWearResponseModel,
    "RobloxAvatarcoreCommonModelsAccessoryPositionModel": RobloxAvatarcoreCommonModelsAccessoryPositionModel,
    "RobloxAvatarcoreCommonModelsAccessoryRefinementModel": RobloxAvatarcoreCommonModelsAccessoryRefinementModel,
    "RobloxAvatarcoreCommonModelsAccessoryRotationModel": RobloxAvatarcoreCommonModelsAccessoryRotationModel,
    "RobloxAvatarcoreCommonModelsAccessoryScaleModel": RobloxAvatarcoreCommonModelsAccessoryScaleModel,
    "RobloxAvatarcoreSharedV3Beta1AssetPosition": RobloxAvatarcoreSharedV3Beta1AssetPosition,
    "RobloxAvatarcoreSharedV3Beta1AssetRotation": RobloxAvatarcoreSharedV3Beta1AssetRotation,
    "RobloxAvatarcoreSharedV3Beta1AssetScale": RobloxAvatarcoreSharedV3Beta1AssetScale,
    "RobloxWebResponsesAvatarScaleModel": RobloxWebResponsesAvatarScaleModel,
    "RobloxWebWebAPIModelsApiLegacyPageResponseRobloxApiAvatarModelsRecentItemModel": RobloxWebWebAPIModelsApiLegacyPageResponseRobloxApiAvatarModelsRecentItemModel,
}

type MimeTypeDescriptor = {
    type: string;
    subtype: string;
    subtypeTokens: string[];
};

/**
 * Every mime-type consists of a type, subtype, and optional parameters.
 * The subtype can be composite, including information about the content format.
 * For example: `application/json-patch+json`, `application/merge-patch+json`.
 *
 * This helper transforms a string mime-type into an internal representation.
 * This simplifies the implementation of predicates that in turn define common rules for parsing or stringifying
 * the payload.
 */
const parseMimeType = (mimeType: string): MimeTypeDescriptor => {
    const [type, subtype] = mimeType.split('/');
    return {
        type,
        subtype,
        subtypeTokens: subtype.split('+'),
    };
};

type MimeTypePredicate = (mimeType: string) => boolean;

// This factory creates a predicate function that checks a string mime-type against defined rules.
const mimeTypePredicateFactory = (predicate: (descriptor: MimeTypeDescriptor) => boolean): MimeTypePredicate => (mimeType) => predicate(parseMimeType(mimeType));

// Use this factory when you need to define a simple predicate based only on type and, if applicable, subtype.
const mimeTypeSimplePredicateFactory = (type: string, subtype?: string): MimeTypePredicate => mimeTypePredicateFactory((descriptor) => {
    if (descriptor.type !== type) return false;
    if (subtype != null && descriptor.subtype !== subtype) return false;
    return true;
});

// Creating a set of named predicates that will help us determine how to handle different mime-types
const isTextLikeMimeType = mimeTypeSimplePredicateFactory('text');
const isJsonMimeType = mimeTypeSimplePredicateFactory('application', 'json');
const isJsonLikeMimeType = mimeTypePredicateFactory((descriptor) => descriptor.type === 'application' && descriptor.subtypeTokens.some((item) => item === 'json'));
const isOctetStreamMimeType = mimeTypeSimplePredicateFactory('application', 'octet-stream');
const isFormUrlencodedMimeType = mimeTypeSimplePredicateFactory('application', 'x-www-form-urlencoded');

// Defining a list of mime-types in the order of prioritization for handling.
const supportedMimeTypePredicatesWithPriority: MimeTypePredicate[] = [
    isJsonMimeType,
    isJsonLikeMimeType,
    isTextLikeMimeType,
    isOctetStreamMimeType,
    isFormUrlencodedMimeType,
];

export class ObjectSerializer {
    public static findCorrectType(data: any, expectedType: string) {
        if (data == undefined) {
            return expectedType;
        } else if (primitives.indexOf(expectedType.toLowerCase()) !== -1) {
            return expectedType;
        } else if (expectedType === "Date") {
            return expectedType;
        } else {
            if (enumsMap.has(expectedType)) {
                return expectedType;
            }

            if (!typeMap[expectedType]) {
                return expectedType; // w/e we don't know the type
            }

            // Check the discriminator
            let discriminatorProperty = typeMap[expectedType].discriminator;
            if (discriminatorProperty == null) {
                return expectedType; // the type does not have a discriminator. use it.
            } else {
                if (data[discriminatorProperty]) {
                    var discriminatorType = data[discriminatorProperty];
                    if(typeMap[discriminatorType]){
                        return discriminatorType; // use the type given in the discriminator
                    } else {
                        return expectedType; // discriminator did not map to a type
                    }
                } else {
                    return expectedType; // discriminator was not present (or an empty string)
                }
            }
        }
    }

    public static serialize(data: any, type: string, format: string) {
        if (data == undefined) {
            return data;
        } else if (primitives.indexOf(type.toLowerCase()) !== -1) {
            return data;
        } else if (type.lastIndexOf("Array<", 0) === 0) { // string.startsWith pre es6
            let subType: string = type.replace("Array<", ""); // Array<Type> => Type>
            subType = subType.substring(0, subType.length - 1); // Type> => Type
            let transformedData: any[] = [];
            for (let date of data) {
                transformedData.push(ObjectSerializer.serialize(date, subType, format));
            }
            return transformedData;
        } else if (type === "Date") {
            if (format == "date") {
                let month = data.getMonth()+1
                month = month < 10 ? "0" + month.toString() : month.toString()
                let day = data.getDate();
                day = day < 10 ? "0" + day.toString() : day.toString();

                return data.getFullYear() + "-" + month + "-" + day;
            } else {
                return data.toISOString();
            }
        } else {
            if (enumsMap.has(type)) {
                return data;
            }
            if (!typeMap[type]) { // in case we dont know the type
                return data;
            }

            // Get the actual type of this object
            type = this.findCorrectType(data, type);

            // get the map for the correct type.
            let attributeTypes = typeMap[type].getAttributeTypeMap();
            let instance: {[index: string]: any} = {};
            for (let attributeType of attributeTypes) {
                instance[attributeType.baseName] = ObjectSerializer.serialize(data[attributeType.name], attributeType.type, attributeType.format);
            }
            return instance;
        }
    }

    public static deserialize(data: any, type: string, format: string) {
        // polymorphism may change the actual type.
        type = ObjectSerializer.findCorrectType(data, type);
        if (data == undefined) {
            return data;
        } else if (primitives.indexOf(type.toLowerCase()) !== -1) {
            return data;
        } else if (type.lastIndexOf("Array<", 0) === 0) { // string.startsWith pre es6
            let subType: string = type.replace("Array<", ""); // Array<Type> => Type>
            subType = subType.substring(0, subType.length - 1); // Type> => Type
            let transformedData: any[] = [];
            for (let date of data) {
                transformedData.push(ObjectSerializer.deserialize(date, subType, format));
            }
            return transformedData;
        } else if (type === "Date") {
            return new Date(data);
        } else {
            if (enumsMap.has(type)) {// is Enum
                return data;
            }

            if (!typeMap[type]) { // dont know the type
                return data;
            }
            let instance = new typeMap[type]();
            let attributeTypes = typeMap[type].getAttributeTypeMap();
            for (let attributeType of attributeTypes) {
                let value = ObjectSerializer.deserialize(data[attributeType.baseName], attributeType.type, attributeType.format);
                if (value !== undefined) {
                    instance[attributeType.name] = value;
                }
            }
            return instance;
        }
    }


    /**
     * Normalize media type
     *
     * We currently do not handle any media types attributes, i.e. anything
     * after a semicolon. All content is assumed to be UTF-8 compatible.
     */
    public static normalizeMediaType(mediaType: string | undefined): string | undefined {
        if (mediaType === undefined) {
            return undefined;
        }
        return mediaType.split(";")[0].trim().toLowerCase();
    }

    /**
     * From a list of possible media types, choose the one we can handle best.
     *
     * The order of the given media types does not have any impact on the choice
     * made.
     */
    public static getPreferredMediaType(mediaTypes: Array<string>): string {
        /** According to OAS 3 we should default to json */
        if (mediaTypes.length === 0) {
            return "application/json";
        }

        const normalMediaTypes = mediaTypes.map(this.normalizeMediaType);

        for (const predicate of supportedMimeTypePredicatesWithPriority) {
            for (const mediaType of normalMediaTypes) {
                if (mediaType != null && predicate(mediaType)) {
                    return mediaType;
                }
            }
        }

        throw new Error("None of the given media types are supported: " + mediaTypes.join(", "));
    }

    /**
     * Convert data to a string according the given media type
     */
    public static stringify(data: any, mediaType: string): string {
        if (isTextLikeMimeType(mediaType)) {
            return String(data);
        }

        if (isJsonLikeMimeType(mediaType)) {
            return JSON.stringify(data);
        }

        throw new Error("The mediaType " + mediaType + " is not supported by ObjectSerializer.stringify.");
    }

    /**
     * Parse data from a string according to the given media type
     */
    public static parse(rawData: string, mediaType: string | undefined) {
        if (mediaType === undefined) {
            throw new Error("Cannot parse content. No Content-Type defined.");
        }

        if (isTextLikeMimeType(mediaType)) {
            return rawData;
        }

        if (isJsonLikeMimeType(mediaType)) {
            return JSON.parse(rawData);
        }

        throw new Error("The mediaType " + mediaType + " is not supported by ObjectSerializer.parse.");
    }
}
